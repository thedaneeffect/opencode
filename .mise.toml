# OpenCode mise configuration

[tools]
bun = "1.3.3"

[tasks.upgrade]
description = "Upgrade opencode (rebuild and install)"
run = "opencode upgrade"

[tasks.sync]
description = "Sync with upstream, rebuild, and install"
run = "opencode upgrade --sync"
